# todo

A simple command-line todo app written in Rust.

## Usage
```bash
cargo run -- add "Buy milk"
cargo run -- list
cargo run -- done 1
cargo run -- delete 1
```

## Install
```bash
cargo install --path .
```
